# Hatcho 제품 전략 & 수익화

> 작성: 2026-07-23 · 갱신: 2026-07-28 · 출시 전 제품/수익화 방향 정리. 실제 코드베이스 기준.
>
> **2026-07-28 결정(MVP 유료 1탄):** 결제 범위 = **게이트만 + 무료 출시**(StoreKit 결제는 1.1) · 상품 = **번들 1개 'Hatcho Pro'**(비소모성). 지금 실제 구현 = **`ProAccess` 게이트 + 배경 컬러 테마 2종(무료 Nest·Midnight)**. **실제 Pro 테마는 아직 없음 → placeholder 미끼 카드 안 만듦**(2.1 리스크 회피). ProAccess 게이트만 1.1(진짜 월드팩·프리미엄 생명체)용 seam으로 선반영. 리롤(re-hatch)은 미룸(루트박스 3.1.1 + append-only 구조). 상세는 아래 G·H절.

## 앱 정체성

**"Forest × Finch"** — 집중 타이머에 **알 부화 → 생명체 수집/진화 → 성격 대사**를 얹은 습관 게임.

- 핵심 루프: 집중 → 알 6단계 crack → 부화 → 12종 생명체(등급별) → 3단계 진화 → 도감 해금
- 부가: 성격 대사, 통계/스트릭/집중점수, 충전 중 "찌릿" 보너스, 기기 간 동기화(Supabase)
- 비교 앱: Forest(일회성+프리미엄), **Finch(구독 ~$40/년, 펫+자기관리 — 구조가 가장 유사)**, Flora, Opal

---

## A. 수익화 모델 — 구독 중심 (Hatcho Plus)

| 모델 | 적합도 | 근거 |
|---|---|---|
| **구독 (추천)** | ★★★ | 매일 쓰는 습관앱 + 계속 늘어나는 컬렉션 = LTV 높음. Finch가 같은 구조로 성공 |
| 일회성 Pro 언락 | ★★ | Forest 모델. 진입장벽 낮지만 매출 상한. 구독 거부감 유저용 보조로 병행 가능 |
| 가챠 소비재(유료 랜덤 알) | ✗ | **비추천.** 집중앱 = 학생·미성년 다수 → 유료 랜덤은 확률 공개 의무(가이드 3.1.1) + 평판 리스크. 프리미엄은 "지정 구매"로만 |

**추천안: 무료(핵심) + Hatcho Plus 구독** (월 ₩4,900 / 연 ₩29,000, 7일 무료체험) + 선택적 일회성 팩(테마 등).

---

## B. 무료 vs 유료 경계 (심사·윤리의 핵심)

> **핵심 루프는 절대 무료 유지.** Apple은 "핵심 기능 페이월"을 리젝. 타이머·부화·진화·기본 12종·통계·스트릭·대사는 전부 무료.

| 무료 | Hatcho Plus / Pro |
|---|---|
| 집중 타이머(25/50/**75**분), 포모도로 | **커스텀 시간**(임의 N분 자유 설정) |
| 알 부화 · 3단계 진화 | — |
| 기본 12종 수집 | **프리미엄 전용 생명체 · 전용 알 풀** (지정 언락, 랜덤 아님) |
| **배경 컬러 테마**(Nest·Midnight, 2종) | **배경 이미지 월드팩**(사이버시티·동양·우주 — H절) |
| 기본 통계 · 스트릭 | **고급 분석** — 히트맵·주간 리포트·CSV 내보내기 |
| 로컬 저장 | **기기 간 클라우드 동기화** (이미 구현됨) |
| — | **집중 중 앱/알림 차단** (Screen Time API) |

※ 동기화 게이팅은 논쟁적(무료로 두는 앱 많음). 대안: 기본 백업 무료 + "다기기 실시간 동기화"만 Plus.
※ 계정 삭제·데이터 접근은 **무조건 무료**(심사 필수).

---

## C. 유료 기능 우선순위 (이미 만든 것 최대 활용)

| 우선 | 기능 | 근거 | 기술 |
|---|---|---|---|
| **P0** | 프리미엄 생명체 + 테마 | 순수 상방, 핵심 게이팅 0, 수집가 지갑 열림. 랜덤 아닌 지정 언락 → 확률 공개 불필요 | `CreatureSpecies.isPremiumOnly`(문서 설계됨) |
| **P1** | 고급 분석 + 커스텀 시간 | 파워유저 전환 훅 | StatsEngine 재사용, 뷰만 게이팅 |
| **P2** | 집중 중 앱 차단 | 전환율 최상급(Opal/Forest 핵심 유료) | `FamilyControls` 엔타이틀먼트 = **Apple 별도 승인** 필요(리드타임 김) |

**기술 기반**: StoreKit 2 + `EntitlementManager.isActive(_:)` 단일 게이트. MVP는 게이트 인터페이스 + 데이터 플래그만 넣고 전부 무료 출시 → 결제는 1.1에서. **첫 출시를 결제로 지연시키지 말 것.**

> 서버 현황: `profiles.is_premium`(bool) + `profiles.settings`(jsonb) 이미 존재. **클라이언트 StoreKit/페이월/is_premium 읽기는 0** = 그린필드.

---

## D. 수익화와 무관하게 추가하면 좋은 것 (출시 임팩트순)

1. ~~**영어 현지화**~~ — ✅ **완료(2026-07-28)**. 앱을 **English base 단일 언어**로 통일(`sourceLanguage=en`, `knownRegions=en`, 죽은 한국어 orphan ~118키 제거). UI 전부 영어. (리스트 초안 `appstore/app-store-listing-en.md`.) 한국어는 추후 정식 재도입(백업 `Localizable.ko-orphans-backup.json`).
2. **위젯(WidgetKit)** — 홈화면 알/스트릭 위젯. 집중앱 리텐션 필수.
3. **Live Activity / 다이내믹 아일랜드** — 진행 중 타이머를 잠금화면에. 이 앱에 완벽하게 어울림, 재진입률↑.
4. **세션 태그/라벨** — "무엇에 집중?"(공부/작업). 통계 세분화 + 요청 많은 기능. `FocusSessionRecord`에 옵셔널 필드(마이그레이션 안전).
5. **스트릭 리마인더 알림** — FocusNotifier 이미 있음. 리텐션.
6. **App Store 스크린샷/프리뷰** — 부화 폭발·컬렉션 강조 5장.

---

## E. 로드맵

| 버전 | 내용 |
|---|---|
| **1.0 (지금)** | 현재 기능 + **`ProAccess` 게이트 + 배경 컬러 테마(무료 2종 Nest·Midnight)** + EN 단일화 + 새 앱아이콘 + 심사 블로커 수정. **결제 없음·Pro 콘텐츠 없음**(게이트 인프라만 선반영) |
| **1.1** | StoreKit2 결제 + 프리미엄 생명체/테마 + 고급 분석 |
| **1.2** | Live Activity, 세션 태그 |
| **1.3** | 앱 차단(FamilyControls 승인 후) |

---

## F. App Store 수익화 컴플라이언스 체크
- 유료에 **랜덤 넣지 말 것** (넣으면 확률 공개 의무, 가이드 3.1.1)
- **핵심 무료 유지** (타이머가 페이월 뒤면 리젝)
- 구독 화면 필수요소: 가격·기간·자동갱신·복원 버튼·약관/개인정보 링크
- 계정 삭제 유지(이미 있음) + 데이터 접근 무료

---

## G. 구현 현황 — MVP 유료 1탄 (2026-07-28)

첫 유료 씨앗 = **`ProAccess` 게이트 + 배경 컬러 테마**. 결제는 없음(게이트만), 1.1 StoreKit이 이 seam에 꽂힌다.

- **코드**: `Services/ProAccess.swift`(게이트) · `DesignSystem/AppTheme.swift`(테마 enum + `ThemedBackground` 뷰) · `Features/Settings/ThemePickerView.swift`(피커). 저장 = `@AppStorage`. **`@Model` 스키마 무변경 → 마이그레이션 리스크 0.**
- **테마**: 무료 `Nest`·`Midnight` 2종만 출시(즉시 선택). **Pro/잠금 테마는 실제 콘텐츠(이미지 월드팩) 준비 전까지 case 자체를 안 넣음** — placeholder 미노출. 화면 루트 배경색만 스왑(카드·탭바·시맨틱색 고정, 다크 유지). 기본 `Nest`는 기존 배경색 재사용 → 회귀 0.
- **잠금 UI 인프라는 유지**(dormant): `AppTheme.isPro`/`locked` + 피커의 "출시 예정" 렌더 + ProAccess 게이트. 1.1에서 실제 Pro 테마 case에 `isPro=true`만 붙이면 자동 동작.
- **게이트**: `ProAccess.isPro`. 1.0 = 항상 false(잠금 = "출시 예정"). DEBUG 토글로 검수 시 미리보기.
- **1.1 seam**: `isPro`를 StoreKit `Transaction.currentEntitlements`로 채우도록 `ProAccess` 구현만 교체 — 호출부(피커) 불변. 잠금 테마가 자동으로 선택 가능해진다.

## H. 로드맵 — 세계관 · 월드팩 · 리롤

### H-1. 이미지 월드팩 (세계관 확장)
배경 테마 시스템을 **이미지 wallpaper**로 확장. 각 "월드" = 배경팩 + **월드 전용 생명체** + (선택)전용 대사 톤.
- **사이버시티**(cyber) — 네온 야경/사이버펑크 · **동양 세계**(zen) — 수묵·한지/정적 · **우주 세계**(cosmos) — 성운/심우주.
- 지금 `AppTheme`의 cyber/zen/cosmos는 이 월드들의 **색 예고편**(출시 예정 잠금). 아트(wallpaper PNG) 준비되면 각 월드를 Pro 팩으로 승격.
- **확률 안전(CRITICAL)**: 월드 전용 생명체는 **가챠 roll 풀에서 제외**(비-rollable). 지정 언락(구매)으로만 획득 → 기존 등급 확률표(`Rarity.tierWeight` 합 100) 불변. 구현 시 `CreatureSpecies`에 unrollable 플래그 + `roll` 풀 필터, `HatchedCreatureRecord`에 origin/source **옵셔널** 필드(lightweight 마이그레이션 안전). **아트+결제 준비 후 착수**(지금 inert 기계 안 만듦 — Simplicity First).
- 이미지 배경 위 카드/텍스트 가독을 위해 소프트 딤 레이어가 필요할 수 있음(디자인 시 확인).

### H-2. 리롤(re-hatch) — 미룸
- **미룬 이유**: (1) 유료 랜덤 = App Store **루트박스(3.1.1)** → 확률 공개 의무. (2) 시스템이 **append-only + union-merge**라 부화 개체를 로컬만 삭제하면 다음 로그인 동기화에서 **부활**(resurrection) → Supabase 삭제 엔드포인트까지 신규 필요(in-mem·SwiftData·Supabase **3레이어 delete 배관**).
- **elegant 구현 경로(추후)**: 부화 **커밋을 'Keep' 탭까지 지연**. 리빌은 미커밋 값만 노출, 리롤 = 미커밋 값 재추첨(delete 불필요), 'Keep' 눌러야 SwiftData insert + Supabase push → append-only 불변 유지. 유료화하려면 **구독 퍼크**로만(개별 랜덤 판매 X) + `DRAW_ODDS` 인앱 노출.

### H-3. 프리미엄 생명체
- H-1과 동일한 확률 안전 원칙(unrollable + 지정 언락). 아트 준비 시 Pro 번들에 편입. 도감 그리드의 잠금 카피는 현재 "Focus to hatch an egg…"라 프리미엄엔 부적합 → "출시 예정/구매" CTA로 특수 처리 필요.
