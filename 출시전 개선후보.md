# 출시 전 개선 후보

> 작성: 2026-07-23 · **아직 코드 미변경.** 출시 전 처리할 항목 모음. 우선순위/담당 표기.
> 담당: `코드`=클로드가 수정 가능 · `ASC`=App Store Connect 설정 · `운영`=호스팅/외부 · `결정`=유저 판단 필요

---

## 0. 유저가 지정한 항목

| # | 항목 | 현재 상태 | 조치 | 담당 |
|---|---|---|---|---|
| U1 | **10초 테스트 프리셋 제거** | ⚠️ 이미 `#if DEBUG`라 **릴리스 빌드엔 안 들어감**(`HomeView.swift:74-78`). 포모도로 고속(10s/5s/30s)도 `SessionManager.swift:17-25` DEBUG 전용 | 릴리스엔 이미 없음. 코드 자체를 완전 삭제할지는 선택 | 코드/결정 |
| U2 | **개발자 디버그 모드 제거** | ⚠️ 전부 `#if DEBUG` 게이팅 확인됨 — `CreatureGalleryView`, `DebugHatch`(강제 부화 종 + **강제 draw `forcedDraws`**), `CREATURE_GALLERY`/`START_TAB` env, MyPage `debugSection`(부화 강제 카드 + **부화 draw 강제 카드**), `ReviewGalleryView`. **릴리스 빌드엔 없음** | 릴리스 안전. 완전 제거 원하면 코드 삭제, 아니면 그대로 둬도 출시 문제 없음 | 코드/결정 |
| ~~U3~~ | ✅ **완료(2026-07-28)** — **English base 단일 언어**로 통일 | `sourceLanguage=en`·`knownRegions=en`, 죽은 한국어 orphan ~118키 제거(실사용 UI 문자열은 이미 전부 영어 키였음). 클린빌드 = en.lproj만. 한국어는 백업(`Localizable.ko-orphans-backup.json`) 후 추후 정식 재도입 | 완료 |
| U4 | **progress(진행 표시) 개선** | 유저 미착수 영역 | 알 crack 진행/타이머 진행 UI 개선. 구체 범위는 추후 정의 | 코드 |

> **참고**: U1·U2는 이미 릴리스에 안 나가므로 "출시 블로커 아님". 코드 청결 목적의 완전 제거만 선택 사항.

---

## 1. 제출 블로커 (반드시 수정)

| # | 항목 | 위치 | 조치 | 담당 |
|---|---|---|---|---|
| ~~B1~~ | ✅ **완료(2026-07-28)** — `ios/Eggtimer/PrivacyInfo.xcprivacy` 생성, 앱 루트 번들 확인. UserDefaults `CA92.1` + 수집(이메일·유저ID·이용기록, linked·앱기능·비추적). ⚠️ B2(ASC 영양표)와 **선언 일치 필수** | 완료 |
| B2 | **App Privacy 영양성분표 미설정** | ASC | 로그인 시 전송: 집중이력·컬렉션·이메일·유저ID → "identity 연결, 앱기능용, 추적 아님". (ATT 불필요 확인됨) | ASC |
| ~~B3~~ | ✅ **완료(2026-07-28)** — `do/catch` + in-memory 폴백. 온디스크 파일 보존(데이터 안전), 마이그레이션 실패해도 런치 크래시 방지 | `EggtimerApp.swift` | 완료 |
| B4 | **개인정보/지원 URL 호스팅** | `docs/privacy-policy.md`, `docs/support.md` | 내용 완비. 실제 공개 URL 필요(GitHub Pages 등) | 운영 |

---

## 2. 리젝 위험 / 정확성

| # | 항목 | 위치 | 조치 | 담당 |
|---|---|---|---|---|
| ~~R1~~ | ✅ **해결** — Apple 단독 로그인 확정(2026-07-23). `docs/privacy-policy.md`에서 Google 언급 3곳 제거 완료(리스트엔 원래 없음). `GoogleAuth` 코드는 `#if canImport`로 이미 컴파일 제외됨(SDK 미링크) — "일단" 미사용, 향후 재도입 여지로 코드 유지 | — | 완료 |
| ~~R2~~ | ✅ **완료(2026-07-28)** — `SecRandomCopyBytes` 반환 `guard errSecSuccess`, 실패 시 트랩(예측가능 nonce 차단) | `AppleSignInNonce.swift` | 완료 |
| ~~R3~~ | ✅ **완료(2026-07-28)** — U3 EN 통일에 흡수. 카탈로그 en-only 재작성, 미완/누락 해소 | `Localizable.xcstrings` | — | 완료 |
| ~~R4~~ | ✅ **완료(2026-07-28)** — 앱명 `Hatcho` 확정(CLAUDE.md 단일출처화). 문제의 "hatchly" 알럿은 이미 stale였고 EN 통일 시 제거됨. 현재 유저노출 문자열에 앱명 불일치 없음. ASC 앱명만 확인 | 완료 |

---

## 3. 정리 / 하드닝 (선택)

- **H1** `VersionedSchema`/`SchemaMigrationPlan` — 스키마 단순할 때 미리 스캐폴딩 (B3와 결합 시 업데이트 크래시 확정 방지)
- **H2** 로그인 후 동기화 실패 조용히 삼킴(`SyncCoordinator.syncOnLogin` 전부 `try?`) → "동기화 켜짐" 오표시. "재시도" 상태 노출 고려
- **H3 (dead code 삭제)**:
  - `SessionManager.eggStageIndex` — 알 리팩터 후 미사용
  - `EggState.eggAssetName` — 삭제된 `Egg0…5` 참조
  - `FocusSession`(keptScreenOn 구조체) — 레거시, `FocusSessionResult`로 대체됨. MockData만 사용
  - `EggtimerTests`의 boilerplate `example` 테스트
  - `START_TAB` env 읽기를 `#if DEBUG`로 감싸기(`RootView.swift:36`)
- **H4 (테스트 갭)**:
  - `StatsEngine.weeklyHours` 요일 remap `(wd+5)%7` 무테스트 — 주간차트 오라벨 위험
  - `FocusScore` 페널티 조합 경계(-8/-20/-30)
  - 시계 되감기 안티치트(`ActiveSession.activeSeconds` max(0,…))
  - `applyChargeBonus`(충전 보너스 캡·running 조건)
  - `ActiveSession` UserDefaults 라운드트립(크래시복구, SwiftData 아니라 안전하게 유닛테스트 가능)

---

## 4. 제품 추가 후보 (수익화 무관, 임팩트순)

> 상세는 `제품전략.md` 참조.

1. **영어 현지화** (U3와 동일) — 시장 10배, 최대 ROI
2. **위젯(WidgetKit)** — 홈화면 알/스트릭, 리텐션
3. **Live Activity / 다이내믹 아일랜드** — 진행 중 타이머
4. **세션 태그/라벨** — "무엇에 집중?", 통계 세분화
5. **스트릭 리마인더 알림** — FocusNotifier 재사용

---

## ✅ 검증 완료 — 손댈 것 없음 (참고)

Supabase **RLS 3테이블 전부 본인 행만**(anon 차단, SECURITY DEFINER search_path 방어) · **계정삭제 실제 원격 삭제**(본인 JWT→엣지펑션→cascade, 5.1.1(v) 충족) · Sign in with Apple 엔타이틀먼트 · 로컬 전용 알림(푸시 권한 불필요) · 오프라인 완전 동작 · export compliance(`ITSAppUsesNonExemptEncryption=NO`) · 클라엔 publishable key만(service_role은 엣지펑션 env) · 동기화 idempotent 합집합(로컬 통째 덮어쓰기 없음) · 확률 추첨 합100 · 디버그 코드 전부 `#if DEBUG` 게이팅.
